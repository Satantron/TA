Please place the wallpaper image you attached here as:

  assets/splash_wallpaper.jpg

The `SplashScreen` will load that local file automatically. If the file is not present the screen will fall back to a remote Unsplash image.

To use the attached image, save it with the exact filename above (JPEG) and restart the Expo server.
